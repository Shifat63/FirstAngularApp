//node ModuleImport.js to run
import {Point} from './Class'
import {Like} from './Exercise'

// let point = new Point(6,7)
// //X and Y are properties. They can be used as variables from outside.
// point.X = 10
// point.Y = 12
// point.pointPrint();

let likeObkect = new Like(100, true);
likeObkect.likePressed()
likeObkect.likePressed()
likeObkect.likePressed()
likeObkect.likePressed()
likeObkect.likePressed()
